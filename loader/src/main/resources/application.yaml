spring:
  application:
    name: loader
  datasource:
    url: jdbc:mysql://localhost:3306/user_database
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

loader:
  batch-size: 100